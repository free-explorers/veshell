import 'package:flutter/material.dart';

class BlurredBackgroundImage extends StatelessWidget {
  const BlurredBackgroundImage({required this.child, super.key});
  final Widget child;
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
